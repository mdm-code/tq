# refernce: https://golangci-lint.run/docs/configuration/file/
# validate: golangci-lint config verify
version = "2"

[linters]
default = "standard"
enable = [
]
disable = [
]

[formatters]
enable = [
	"gofmt",
	"gofumpt",
	"goimports",
	"golines",
	"gci",
]

[run]
timeout = "5m"
